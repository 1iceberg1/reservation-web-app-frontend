import { ReservationListView } from 'src/sections/reservation/view';

// ----------------------------------------------------------------------

export const metadata = {
  title: 'Painel de administração: lista de reservas',
};

export default function ReservationListPage() {
  return <ReservationListView />;
}
