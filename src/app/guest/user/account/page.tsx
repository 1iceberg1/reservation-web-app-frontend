import { AccountView } from 'src/sections/account/view';

// ----------------------------------------------------------------------

export const metadata = {
  title: 'Painel do Vendedor: Configurações da Conta',
};

export default function AccountPage() {
  return <AccountView />;
}
