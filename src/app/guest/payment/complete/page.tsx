import { PaymentCompleteView } from 'src/sections/payment/view';

// ----------------------------------------------------------------------

export const metadata = {
  title: 'Painel do Vendedor: Payment Complete',
};

export default function DashboardPage() {
  return <PaymentCompleteView />;
}
