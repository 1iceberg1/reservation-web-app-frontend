import ReservationCheckin from 'src/sections/reservation/view/reservation-checkin';

// ----------------------------------------------------------------------

export const metadata = {
  title: 'Painel do Vendedor: Painel',
};

export default function DashboardPage() {
  return <ReservationCheckin />;
}
