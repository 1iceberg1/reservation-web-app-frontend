import ReservationCheckout from 'src/sections/reservation/view/reservation-checkout';

// ----------------------------------------------------------------------

export const metadata = {
  title: 'Painel do Vendedor: Painel',
};

export default function DashboardPage() {
  return <ReservationCheckout />;
}
