import { UserListView } from 'src/sections/user/view';

// ----------------------------------------------------------------------

export const metadata = {
  title: 'Painel de administração: lista de usuários',
};

export default function UserListPage() {
  return <UserListView />;
}
