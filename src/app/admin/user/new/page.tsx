import { UserCreateView } from 'src/sections/user/view';

// ----------------------------------------------------------------------

export const metadata = {
  title: 'Painel de administração: Crie um novo usuário',
};

export default function UserCreatePage() {
  return <UserCreateView />;
}
