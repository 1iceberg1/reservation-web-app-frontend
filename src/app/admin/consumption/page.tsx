import { ConsumptionListView } from 'src/sections/consumption/view';

// ----------------------------------------------------------------------

export const metadata = {
  title: 'Painel de administração: lista de consumptions',
};

export default function ConsumptionListPage() {
  return <ConsumptionListView />;
}
