import { UserProfileView } from 'src/sections/user/view';

// ----------------------------------------------------------------------

export const metadata = {
  title: 'Painel de administração: perfil do usuário',
};

export default function UserProfilePage() {
  return <UserProfileView />;
}
