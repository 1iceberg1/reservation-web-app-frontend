import { AccountView } from 'src/sections/account/view';

// ----------------------------------------------------------------------

export const metadata = {
  title: 'Painel de administração: configurações da conta',
};

export default function AccountPage() {
  return <AccountView />;
}
